import { Plugin, PluginOption } from 'vite';

interface Options {
    removeAttributeQuotes?: boolean;
    collapseWhitespace?: boolean;
    [attr: string]: any;
}
declare const HtmlMinifierTerser: (options?: Options) => Plugin | PluginOption;

export { HtmlMinifierTerser as default };
